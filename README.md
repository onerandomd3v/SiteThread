# SiteThread

SiteThread is a new project.

## Branches

- `main` is the protected release branch.
- `dev` is the protected development and pull-request integration branch.

Changes should be developed on short-lived feature branches and merged through pull requests.
