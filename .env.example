# Server-only values. Never prefix these with NEXT_PUBLIC_.
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

R2_ACCOUNT_ID=""
R2_BUCKET_NAME="sitethread-media"
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""

TRIGGER_SECRET_KEY=""
TRIGGER_PROJECT_REF=""

# COD-14 selected raw MCP. The bearer is optional for keyless validation,
# but production access must be validated by COD-32 before the live pipeline.
LIVEPEER_MCP_URL="https://agent.livepeer.org/api/mcp"
LIVEPEER_MCP_BEARER=""
MEDIA_PROVIDER_MODE="fixture"
