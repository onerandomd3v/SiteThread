# Server-only values. Never prefix these with NEXT_PUBLIC_.
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

R2_ACCOUNT_ID=""
R2_BUCKET_NAME="sitethread-media"
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""

TRIGGER_SECRET_KEY=""
TRIGGER_PROJECT_REF=""

# COD-32 selected the path-pinned raw MCP endpoint. Keep this example bearer blank;
# keyless access is for development and validation only. Validate an issued bearer
# with a deployment smoke test before enabling the live demo or production provider.
LIVEPEER_MCP_URL="https://agent.livepeer.org/api/mcp/raw"
LIVEPEER_MCP_BEARER=""
MEDIA_PROVIDER_MODE="fixture"
